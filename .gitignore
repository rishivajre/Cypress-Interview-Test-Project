# Cypress recommended .gitignore

# Ignore Node.js dependencies
node_modules/

# Ignore Cypress-related files
# Cypress generates videos and screenshots during test runs.
# These can sometimes capture sensitive information and should be ignored.
cypress/screenshots/
cypress/videos/
cypress/downloads/
.env
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# VS Code settings
.vscode/

# Allure results folder
# Contains the raw data collected during the test run.
allure-results/

# Allure report folder
# The final HTML report, generated from the results.
allure-report/
reports/*

# Keep only the sample report
!reports/allure-sample/